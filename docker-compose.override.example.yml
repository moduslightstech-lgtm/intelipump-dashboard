# Local-only overrides — do not use on the DigitalOcean droplet.
services:
  postgres:
    ports:
      # 5433 avoids clashing with other local Postgres containers on 5432
      - "127.0.0.1:5433:5432"

  api:
    ports:
      - "127.0.0.1:8000:8000"
