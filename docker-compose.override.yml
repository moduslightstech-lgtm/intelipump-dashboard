# Local-only overrides — do not copy this file to the DigitalOcean droplet.
# On the droplet the API must stay on the docker network (nginx :80 only).
services:
  postgres:
    ports:
      - "127.0.0.1:5433:5432"

  api:
    ports:
      - "127.0.0.1:8000:8000"
